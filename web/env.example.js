// Archivo de configuración de entorno para producción
// Renombrar a env.js y modificar según necesidad
// Este archivo debe incluirse ANTES de config.js en el HTML

window.ENV = {
  // URL del API backend (ejemplo: https://api.diprochil.cl)
  API_URL: 'http://localhost:3000',

  // Otras configuraciones de entorno si es necesario
  // ENVIRONMENT: 'production',
};
